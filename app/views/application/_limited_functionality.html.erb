<% if current_workspace.expired? %>
  <div class="relative w-full">
    <div 
      class="absolute top-0 right-0 inline-flex rounded-lg items-center justify-end bg-amber-400 mr-3" 
      title="Features are reduced because your workspace has expired"
    >
      <p class="py-1 px-2 text-sm whitespace-nowrap text-gray-800">Limited Functionality</p>
    </div>
  </div>
<% end %> 