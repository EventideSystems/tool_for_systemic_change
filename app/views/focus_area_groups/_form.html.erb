<%= simple_form_for(@focus_area_group, role: 'form', html: { class: 'form-horizontal record-form' }) do |f| %>
  <%= f.error_notification %>
  <%= form_header(@focus_area_group) %>
  <%= f.input :name %>
  <%= f.input :description , as: :string %>
  <%= f.input :scorecard_type, collection: options_for_scorecard_type, label: 'Scorecard Type', include_blank: false %>
  <%= f.input :position %>
  <%= render_form_button(f) %>
<% end %>
