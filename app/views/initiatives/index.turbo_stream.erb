<%= turbo_stream.append :initiatives do %>
  <%= render @initiatives %>
<% end %>
<%= turbo_stream.replace :pagination do %>
  <% if @pagy.next.present? %>
    <%=
      turbo_frame_tag(
        :pagination,
        loading: :lazy,
        src: initiatives_path(format: :turbo_stream, page: @pagy.next)
      )
    %>
  <% end %>
<% end %>
