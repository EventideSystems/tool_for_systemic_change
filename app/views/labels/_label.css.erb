

.<%= dom_id(label) %> {
  color: <%= label.color %>;
  <%- if calculate_luminance(label.color) > 0.06 %>
    background-color: oklch(from <%= label.color %> l c h / 0.1);
  <%- else %>
    background-color: <%= contrasting_text_color(label.color) %>;
  <%- end %>
}
