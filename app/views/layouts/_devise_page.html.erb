<div class="flex min-h-screen flex-col justify-center items-center py-12 sm:px-6 lg:px-8">
  <div class="flex items-center justify-center space-x-4">
    <%= image_tag 'logo-small.png', class: 'h-12 w-auto', alt: "Logo for the 'Tool for Systemic Change'" %>
    <h2 class="text-3xl font-bold tracking-tight text-gray-900 dark:text-white"><%= title %></h2>
  </div>

  <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
    <div class="bg-white py-8 px-4 shadow sm:rounded-lg sm:px-10">
      <%= yield %>
    </div>
  </div>
</div>
