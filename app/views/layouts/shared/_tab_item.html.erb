<%
  if active
    options = {
      class: "rounded-md bg-black dark:bg-gray-100 px-3 py-2 text-sm font-medium text-white dark:text-gray-700",
      aria: { current: "page" }
    }
  else
    options = { class: "rounded-md px-3 py-2 text-sm font-medium text-gray-500 hover:text-gray-700" }
  end
%>

<%= link_to path, options do %>
  <%= title %>
<% end %>


