<%= turbo_stream.append :users do %>
  <%= render @users %>
<% end %>
<%= turbo_stream.replace :pagination do %>
  <% if @pagy.next.present? %>
    <%=
      turbo_frame_tag(
        :pagination,
        loading: :lazy,
        src: users_path(format: :turbo_stream, page: @pagy.next)
      )
    %>
  <% end %>
<% end %>
