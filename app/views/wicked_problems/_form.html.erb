<%= form_for(@wicked_problem) do |f| %>
  <% if @wicked_problem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wicked_problem.errors.count, "error") %> prohibited this problem from being saved:</h2>

      <ul>
      <% @wicked_problem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
