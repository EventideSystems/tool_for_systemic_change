require 'rails_helper'

RSpec.describe "Initiatives::Imports", type: :request do
  describe "GET /initiatives_imports" do
    it "works! (now write some real specs)" do
      # get initiatives_imports_path
      # expect(response).to have_http_status(200)
    end
  end
end
